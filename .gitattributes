# Normalize all text to LF in the repo, checkout as-is on each OS
* text=auto eol=lf

# Ensure husky hooks are always LF (bash scripts)
.husky/* text eol=lf

# Binary assets — never touch
*.webp binary
*.png  binary
*.jpg  binary
*.ico  binary
*.woff2 binary
